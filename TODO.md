# TODO

## v1.4.14

- [x] 将未知游戏 build 的运行时处理从整体停用改为逐功能兼容降级。
- [x] 允许已知适配器以真实 Bundle 结构校验替代 build ID/原版哈希硬阻断。
- [x] 原生槽位或 Spine 结构不兼容时继续部署运行时安全槽位，并公开降级警告。
- [x] 取消部署时逐项恢复，保留外部改动文件及对应恢复备份。
- [x] 发布后保留本机 v1.4.13，不自动安装 v1.4.14，供手动验收更新提示、下载、校验与安装交互。

## v1.4.13

- [x] 适配 Steam build `24720155` 的八英雄默认皮肤和语音路由合同。
- [x] 处理 Steam 仅重置 Addressables 目录、保留旧补丁 Bundle 的安全恢复与重新部署。
- [ ] 将版本适配拆成自动采证、差异分级和人工签署三个阶段，避免把未知游戏 ABI 自动加入生产白名单。

## v1.4.12

- [x] BC7 校验允许稀疏高误差 texel，不因单个最大值误杀有效小图标。
- [x] 保留对大面积及高占比严重编码误差的 fail-closed 检测。

## v1.4.11

- [x] Steam 更新后跨英雄重新部署时，使用当前适配器合同识别旧部署目标的新原版 Bundle。
- [x] 保持未知哈希、残留旧补丁或不支持当前 build 的适配器严格拒绝退休事务。

## v1.4.10

- [x] Steam 完整重置原生补丁事务后允许安全重部署，不把旧备份覆盖到新游戏版本。
- [x] 部分重置、未知哈希或仍残留旧补丁目录时继续 fail closed。
- [x] 共享同一原生 Texture2D 的逻辑槽位在部署物化时使用规范槽位图片，消除外部设备上的冲突校验错误。

## v1.4.9

- [x] 空白或失效的逐槽位脏标记不再覆盖已生成的默认槽位。
- [x] 覆盖计数、默认预览和发布路径只使用具有可读取源素材的有效覆盖。
- [x] 保留用户主动导入全透明 PNG 的能力，并增加空状态、丢失源文件和透明源图回归测试。

## v1.4.8

- [x] 逐槽位覆盖首次修改时脱离可变 `assets` 输出，建立不可变源图快照。
- [x] 验证同一缩放与偏移连续物化两次不会平方或累计。
- [x] 保持默认/逐槽位切换的内存快速路径，不在选项卡切换时执行完整构建。

## v1.4.7

- [x] 模式选项卡切换不再执行工作区构建、PNG 编码或 ZIP 导出。
- [x] 使用默认底稿加稀疏逐槽位覆盖作为制作过程中的单一真实状态。
- [x] 默认模式预览和发布操作合并逐槽位修改。
- [x] 逐槽位导入与 ZIP 导出后台执行，并批量保存视觉槽位。
- [x] 在真实素材项目中验收连续往返切换、后台底稿同步和逐槽位覆盖预览；选项卡立即保持目标模式，切换处理不再触发工作区构建。

## v1.4.6

- [x] 逐槽位编辑右侧显示同画布、同缩放的游戏原版素材参考。
- [x] 原版参考优先使用已验证的原生 Bundle 备份，不读取当前已部署皮肤。
- [x] 动态 / 分层且没有可信静态位图的槽位明确降级提示。

## v1.4.2

- [x] 动画导入页直接接受 Spine ZIP 包并自动识别运行时版本。
- [x] 兼容多页 Atlas、嵌套源图与附带 `.spine` 工程文件的社区包。
- [x] 使用 `维琳娜.zip` 对源码路径和冻结版 EXE 执行导入冒烟验证。

## v1.4.1

- [x] 升级安装时清理已集成到主程序的两个旧版独立 EXE 与构建元数据。

## v1.4.0

- [x] 为八名英雄建立 build `24570932` 的精确英雄语音路由目录。
- [x] 支持把 Mak/KTN 语音素材按逻辑槽位重定向给其他英雄。
- [x] Runtime 按当前英雄 AudioSO 选择对应皮肤包，并补齐 Pyg/The Dragons 别名。
- [x] 公共构建携带全英雄音频元数据，校验器接受所有受支持英雄。
- [ ] 在游戏中分别抽测八名英雄的 Idle、升级、PvP 开场和胜负语音日志。

## v1.3.3

- [x] 英雄选择图标在逐槽位模式中恢复为原生框与可调人物层合成。
- [x] 游戏内头像预览显示与正式输出相同的三侧裁剪和框上层遮挡。
- [x] 立绘挂载由固定屏幕偏移改为原生/替换投影下缘对齐。
- [ ] 在游戏日志中验收 v1.3.3 的 PvP、一天开始和一天结束下缘测量值。

## v1.3.2

- [x] 制作过程中可随时从默认草稿切换到逐槽位微调，无需先发布或导入。
- [x] 离开逐槽位模式时保存同一工作区缓存，再次进入时继续已有微调。
- [x] 仅当默认模式素材或参数发生变化时重建逐槽位起点，避免无修改切换覆盖微调。
- [x] 默认模式可直接导入皮肤库，不再强制跳转到逐槽位模式。
- [x] Manager 内嵌生成保留共享工作区目录和逐槽位源素材。

## v1.3.1

- [x] 自动生成模式与逐槽位模式统一使用 Manager 工作区缓存。
- [x] 自动模式生成的全部槽位成品图直接成为逐槽位模式的初始素材。
- [x] 对局头像自动恢复为背景与人物前景两层，其他槽位保留完整生成结果。
- [x] 逐槽位保存前先归档缓存图，避免清理旧输出时丢失正在接续的素材。
- [x] 保留自动模式的原始输入与调整参数，逐槽位编辑后仍可重新进入自动模式。
